# Silly Wizards Guild — Decision & Change Log

**Vocabulary.** An entry with a `**Status:**` is a decision. An entry without one is a change.
`APPROVED` — signed · `RECOMMENDED` — awaiting pass / adjust / kill · `OPEN` — undecided, needs work · `SUPERSEDED` — replaced, with a pointer to what replaced it.

Newest first. Append only — never rewrite an entry. A correction is a new entry that supersedes the old one.

---

## 2026-08-18 — Does the Guild stay quiet infrastructure, or does it need a public face?
**Status:** OPEN
**Decision:** Undecided, and deliberately so. Recorded here because it has been an open thread since April and had no home.
**Evidence:** `_Project Notes.md` (on disk) raised it under "Open threads" on 2026-04-23 and it has not moved since. The funding work of 2026-05-27 pulls the other way — external funders want the Guild legible — while the site's whole design assumes it stays quiet.
**Practical consequence:** Until it is decided, nothing on the site gets a service list, a price or a contact funnel. A change in that direction is this decision being made by accident, which is the thing this entry exists to prevent.

## 2026-08-18 — Profile is `build`, not `core`
**Status:** RECOMMENDED
**Decision:** This repo installs at profile `build`, which adds `aios/CANON.md` to the core set.
**Evidence:** It ships a deployed artefact — one page plus twenty-four icons, live at sillywizardsguild.com via Vercel from `main`. No experimental runs, so not `research`; no schedule, so not `operator`.
**Practical consequence:** `aios/CANON.md` is required and is now written by hand. Awaiting the owner's verdict.

## 2026-08-18 — AiOS v2.0 installed in this repo
**Status:** APPROVED
**Decision:** This repo runs AiOS v2.0. `aios/STATE.md` is generated by `bin/aios-state.mjs` and is never hand-edited; a hand edit is check failure `S1`.
**Evidence:** Owner authorised the install in session on 2026-08-18. Canon and tooling live at `~/Desktop/CLAUDE COWORK/AiOS/aios-v2.0`; this repo carries no copy of canon by design.
**Practical consequence:** Sessions begin by reading `aios/STATE.md` and end when the owner says "close the session".
